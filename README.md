# Next.js Web App (PWA) Headless Shopify Backend with GraphQL queries

Commands
```bash
  // Development  
  yarn dev
  
  // Production Build
  yarn build
  
  // Start
  yarn start
  
  // Linting
  yarn lint
  
 ```
